{
  "name": "countrymatcher",
  "private": true,
  "type": "module",
  "scripts": {
    "pretest": "node scripts/ensure-node-deps.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1"
  }
}
